# assets.directory is the repo root ("./"), so exclude everything that is not site content.

# VCS
.git
.gitignore
.gitattributes

# Tooling and dependencies
node_modules
.wrangler
package.json
package-lock.json
wrangler.jsonc

# Docs
CLAUDE.md
README.md
DESIGN.md

# Secrets and local config
.env*
.dev.vars*

# Logs and OS/editor files
*.log
.DS_Store
Thumbs.db
.vscode
.idea
*.swp
